<div class="page-header">
	<h3> <%= link_to "Voltar", aquaviario_dads_path %> | Lista de Unidades de Carga do D.A.D. Numero <%= @dad.id %> </h3>
</div>

<table class="table table-bordered table-striped table-hover">
	<thead>
		<tr><td><strong>Transportador Aquaviario:</strong> <%= @dad.empresa.nome_fantasia %></td></tr>
		<tr><td><strong>D.A.D. Numero: </strong> <%= @dad.id %> <strong>Status: </strong><%= @dad.status_dad.descricao %></td></tr>
		<tr><td><strong>Empurrador:</strong> <%= @dad.empurrador %>	<strong>Balsa:</strong> <%= @dad.balsa %></td></tr>
	</thead>
</table>
<table class="table table-bordered table-striped table-hover">
	<thead>
		
		<tr>
			
			
			<th>Identificação</th>
			
			<th>Tipo unidade de carga</th>
			
			<th>QDTE documentos eletronicos</th>
			
			<th>Criado Por</th>

						
			<th ><%= render "search" %></th>
		</tr>
	</thead>

	<tbody>
		<%= content_tag_for(:tr, @auc) do |aquaviario_unidade_carga| %>
		
		<td><%= aquaviario_unidade_carga.placa %></td>
		
		<td><%= aquaviario_unidade_carga.tipo_unidade.descricao %></td>
		
		<td><%= aquaviario_unidade_carga.dcs.size %></td>
		
		<td><%= aquaviario_unidade_carga.usuario.nome %> - <%= aquaviario_unidade_carga.created_at.to_s_br %></td>
			
		<td><%= link_to 'Mostrar', aquaviario_dad_unidade_carga_path(@dad, aquaviario_unidade_carga), :class=>"btn btn-small"%></td>
		<td><%= link_to 'Editar', edit_aquaviario_dad_unidade_carga_path(@dad, aquaviario_unidade_carga), :class=>"btn btn-primary btn-small" %></td>
		<td><%= link_to 'Apagar', aquaviario_dad_unidade_carga_path(@dad, aquaviario_unidade_carga), :class=>"btn btn-danger btn-small", method: :delete, data: { confirm: 'Você está certo disso?' } %></td>
		<td><%= link_to "Documento Eletronico (#{aquaviario_unidade_carga.dcs.size})", aquaviario_dad_unidade_carga_dcs_path(@dad, aquaviario_unidade_carga), :class=>"btn btn-success btn-small" %></td>
		
		
		<% end %>
	</tbody>
</table>

<br />
<% if @dad.status_dad != 10003 %>
	<%= link_to 'Nova Unidade Carga',  new_aquaviario_dad_unidade_carga_path(@dad), :class=>"btn btn-success" %>
<% end %>
