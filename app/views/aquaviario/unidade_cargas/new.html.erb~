<div class="page-header">
	<h3><%= link_to 'Voltar', aquaviario_dad_unidade_cargas_path(@dad) %> | Novo(a) aquaviario_unidade_carga</h3>
</div>

<div class="row">
	<div class="span10 offset1">

		<%= form_for(@aquaviario_unidade_carga, :url=>aquaviario_dad_unidade_cargas_path(@dad), :html=>{:class=>"form-horizontal well"}) do |f| %>
		<% if @aquaviario_unidade_carga.errors.any? %>
		<div class="alert alert-error">
			<button type="button" class="close" data-dismiss="alert">&times;</button>
			<strong><%= pluralize(@aquaviario_unidade_carga.errors.count, "erro") %> ao tentar salvar este formulario:</strong>
			
			<ul>
				<% @aquaviario_unidade_carga.errors.full_messages.each do |msg| %>
				<li>
					<%= msg %>
				</li>
				<% end %>
			</ul>
		</div>
		<% end %>
		
		<%= f.hidden_field :dad_id, :value=>@dad.id %>
		<%= f.hidden_field :usuario_id, :value=>@dad.id %>
				
		<div class="control-group">
			<%= f.label :placa, "Identificacão Unidade de Carga", :class=>"control-label" %>
			<div class="controls">
				<%= f.number_field :placa, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :tipo_unidade_id, "Tipo Unidade de Carga", :class=>"control-label" %>
			<div class="controls">
				<%= f.collection_select(:tipo_unidade_id, Core::TipoUnidade.order('descricao'),
										:id, :descricao, {:prompt => true}, 
										:required=>true, :class=>"input-xlarge") 
    			%>
				<p class="help-block">
					
				</p>
			</div>
		</div>
			
		<div class="form-actions">
			<%= f.submit "Salvar", :class=>"btn btn-primary" %>
			<button type="reset" class="btn">
				Cancelar
			</button>
		</div>

		
		<% end %>

	</div>
</div>

<%= link_to 'Voltar', aquaviario_dad_unidade_cargas_path(@dad) %>

