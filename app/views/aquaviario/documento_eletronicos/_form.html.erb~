<div class="row">
	<div class="span10 offset1">

		<%= form_for(@aquaviario_documento_eletronico, :html=>{:class=>"form-horizontal well"}) do |f| %>
		<% if @aquaviario_documento_eletronico.errors.any? %>
		<div class="alert alert-error">
			<button type="button" class="close" data-dismiss="alert">&times;</button>
			<strong><%= pluralize(@aquaviario_documento_eletronico.errors.count, "erro") %> ao tentar salvar este formulario:</strong>
			
			<ul>
				<% @aquaviario_documento_eletronico.errors.full_messages.each do |msg| %>
				<li>
					<%= msg %>
				</li>
				<% end %>
			</ul>
		</div>
		<% end %>
				
		<%= f.hidden_field :dad_id, :value=>@dad.id %>		
		<%= f.hidden_field :unidade_carga_id, :value=>@uc.id %>
		<%= f.hidden_field :usuario_id, :value=>@usuario_logado.id %>
				
		<div class="control-group">
			<%= f.label :rodoviario_id, :class=>"control-label" %>
			<div class="controls">
				<%= f.number_field :rodoviario_id, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :tipo_documento_eletronico_id, :class=>"control-label" %>
			<div class="controls">
				<%= f.number_field :tipo_documento_eletronico_id, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :documento, :class=>"control-label" %>
			<div class="controls">
				<%= f.text_field :documento, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
				
		<div class="form-actions">
			<%= f.submit :class=>"btn btn-primary" %>
			<button type="reset" class="btn">
				Cancelar
			</button>
		</div>
		<% end %>

	</div>
</div>
