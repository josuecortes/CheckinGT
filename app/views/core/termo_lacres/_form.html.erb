<div class="row">
	<div class="span10 offset1">

		<%= form_for(@core_termo_lacre, :html=>{:class=>"form-horizontal well"}) do |f| %>
		<% if @core_termo_lacre.errors.any? %>
		<div class="alert alert-error">
			<button type="button" class="close" data-dismiss="alert">&times;</button>
			<strong><%= pluralize(@core_termo_lacre.errors.count, "erro") %> ao tentar salvar este formulario:</strong>
			
			<ul>
				<% @core_termo_lacre.errors.full_messages.each do |msg| %>
				<li>
					<%= msg %>
				</li>
				<% end %>
			</ul>
		</div>
		<% end %>
				
		
		<div class="control-group">
			<%= f.label :dad_id, :class=>"control-label" %>
			<div class="controls">
				<%= f.number_field :dad_id, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :unidade_carga_id, :class=>"control-label" %>
			<div class="controls">
				<%= f.number_field :unidade_carga_id, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :fisco_cpf_id, :class=>"control-label" %>
			<div class="controls">
				<%= f.number_field :fisco_cpf_id, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :termo_lacre_data, :class=>"control-label" %>
			<div class="controls">
				<%= f.datetime_select :termo_lacre_data, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :termo_lacre_numero, :class=>"control-label" %>
			<div class="controls">
				<%= f.text_field :termo_lacre_numero, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :observacoes, :class=>"control-label" %>
			<div class="controls">
				<%= f.text_field :observacoes, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="form-actions">
			<%= f.submit :class=>"btn btn-primary" %>
			<button type="reset" class="btn">
				Cancelar
			</button>
		</div>
		<% end %>

	</div>
</div>
