<div class="row">
	<div class="span10 offset1">

		<%= form_for(@core_uf, :html=>{:class=>"form-horizontal well"}) do |f| %>
		<% if @core_uf.errors.any? %>
		<div class="alert alert-error">
			<button type="button" class="close" data-dismiss="alert">&times;</button>
			<strong><%= pluralize(@core_uf.errors.count, "erro") %> ao tentar salvar este formulario:</strong>
			
			<ul>
				<% @core_uf.errors.full_messages.each do |msg| %>
				<li>
					<%= msg %>
				</li>
				<% end %>
			</ul>
		</div>
		<% end %>
				
		
		<div class="control-group">
			<%= f.label :sigla, :class=>"control-label" %>
			<div class="controls">
				<%= f.text_field :sigla, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="form-actions">
			<%= f.submit :class=>"btn btn-primary" %>
			<button type="reset" class="btn">
				Cancelar
			</button>
		</div>
		<% end %>

	</div>
</div>
