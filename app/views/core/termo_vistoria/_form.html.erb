<div class="row">
	<div class="span10 offset1">

		<%= form_for(@core_termo_vistorium, :html=>{:class=>"form-horizontal well"}) do |f| %>
		<% if @core_termo_vistorium.errors.any? %>
		<div class="alert alert-error">
			<button type="button" class="close" data-dismiss="alert">&times;</button>
			<strong><%= pluralize(@core_termo_vistorium.errors.count, "erro") %> ao tentar salvar este formulario:</strong>
			
			<ul>
				<% @core_termo_vistorium.errors.full_messages.each do |msg| %>
				<li>
					<%= msg %>
				</li>
				<% end %>
			</ul>
		</div>
		<% end %>
				
		
		<div class="control-group">
			<%= f.label :dad_id, :class=>"control-label" %>
			<div class="controls">
				<%= f.number_field :dad_id, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :unidade_carga_id, :class=>"control-label" %>
			<div class="controls">
				<%= f.number_field :unidade_carga_id, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :termo_vistoria_data_inicio, :class=>"control-label" %>
			<div class="controls">
				<%= f.datetime_select :termo_vistoria_data_inicio, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :termo_vistoria_data_final, :class=>"control-label" %>
			<div class="controls">
				<%= f.datetime_select :termo_vistoria_data_final, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :observacoes, :class=>"control-label" %>
			<div class="controls">
				<%= f.text_field :observacoes, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :termo_contagem, :class=>"control-label" %>
			<div class="controls">
				<%= f.text_field :termo_contagem, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="form-actions">
			<%= f.submit :class=>"btn btn-primary" %>
			<button type="reset" class="btn">
				Cancelar
			</button>
		</div>
		<% end %>

	</div>
</div>
