<div class="row">
	<div class="span10 offset1">

		<%= form_for(@core_selo_fiscal, :html=>{:class=>"form-horizontal well"}) do |f| %>
		<% if @core_selo_fiscal.errors.any? %>
		<div class="alert alert-error">
			<button type="button" class="close" data-dismiss="alert">&times;</button>
			<strong><%= pluralize(@core_selo_fiscal.errors.count, "erro") %> ao tentar salvar este formulario:</strong>
			
			<ul>
				<% @core_selo_fiscal.errors.full_messages.each do |msg| %>
				<li>
					<%= msg %>
				</li>
				<% end %>
			</ul>
		</div>
		<% end %>
				
		
		<div class="control-group">
			<%= f.label :status_selo_fiscal_tipo_id, :class=>"control-label" %>
			<div class="controls">
				<%= f.number_field :status_selo_fiscal_tipo_id, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :selo_fiscal_data, :class=>"control-label" %>
			<div class="controls">
				<%= f.datetime_select :selo_fiscal_data, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="control-group">
			<%= f.label :descricao, :class=>"control-label" %>
			<div class="controls">
				<%= f.text_field :descricao, :class=>"input-xlarge" %>
				<p class="help-block">
					
				</p>
			</div>
		</div>
		
		<div class="form-actions">
			<%= f.submit :class=>"btn btn-primary" %>
			<button type="reset" class="btn">
				Cancelar
			</button>
		</div>
		<% end %>

	</div>
</div>
