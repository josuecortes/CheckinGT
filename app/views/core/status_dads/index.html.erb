<div class="page-header">
	<h3>Lista de core_status_dads</h3>
</div>

<table class="table table-bordered table-striped table-hover">
	<thead>
		<tr>
			
			<th>Descricao</th>
			
			<th></th>
			<th></th>
			<th></th>
		</tr>
	</thead>

	<tbody>
		<%= content_tag_for(:tr, @core_status_dads) do |core_status_dad| %>
		
		<td><%= core_status_dad.descricao %></td>
		
		<td><%= link_to 'Mostrar', core_status_dad, :class=>"btn btn-small"%></td>
		<td><%= link_to 'Editar', edit_core_status_dad_path(core_status_dad), :class=>"btn btn-primary btn-small" %></td>
		<td><%= link_to 'Apagar', core_status_dad, :class=>"btn btn-danger btn-small", method: :delete, data: { confirm: 'Você está certo disso?' } %></td>
		<% end %>
	</tbody>
</table>

<br />

<%= link_to 'Novo(a) Status dad', new_core_status_dad_path, :class=>"btn btn-success" %>

